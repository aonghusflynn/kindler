<%= form_for(@krindle) do |f| %>
  <% if @krindle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@krindle.errors.count, "error") %> prohibited this krindle from being saved:</h2>

      <ul>
      <% @krindle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, "Name" %>
  <%= f.text_field :name, :size=>40 %>

  <%= f.label :message, "Message" %>
  <%= f.text_area :message, :size=>40 %>
  
  <%= f.label :max_amount, "Maximum amount to spend on a gift" %>
  <%= f.text_field :max_amount, :size=>40 %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
